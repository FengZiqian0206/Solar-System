# Process / 我和 AI 如何合作

## 我的工作与 AI 的工作

我从 ECharts 三维散点示例出发，确定了“太阳系漫游指南”的主题、黑白双语界面与彩色半透明粒子的视觉方向。我持续测试实际效果，提出并确认修改：粒子大小和透明度、三档密度、行星间距、土星环与小行星带、呼吸与流星、行星后方的阴影、拖动方向、跟随视角，以及数据图表的整合。

AI 承担了大部分代码编写与调整，包括 Three.js 渲染、交互界面、运动计算、数据图表和兼容性修复。我负责判断结果是否符合预期，指出误解并决定保留、回退或继续调整。协作方式是“提出具体目标 → AI 修改 → 我在浏览器中体验 → 反馈问题 → 再修改”，并非一次生成后直接接受。

## 使用的工具

- ECharts / ECharts GL：最初的三维散点参考；Three.js：网页三维点云与交互。
- JavaScript、HTML、CSS 与 SVG：动画、界面、标注和整合后的数据图表。
- Git / GitHub / GitHub Pages：版本记录、仓库与公开网页；浏览器：检查实际显示和操作。
- Python 标准库：本次补充的 `fetch.py` 用于下载原始数据；`plot.py` 用于读取快照并生成独立 SVG。不使用 pandas，也不要求额外安装绘图库。

## 保留的一件事

我保留了黑白界面与彩色粒子的分工，以及红色点更大、更不透明的视觉规则。它让界面不抢主体，又让星球中心、边缘和宇宙空间形成层次。数据表和网页图表继续复用原有行星数据，避免不同位置各维护一套数字。

## 拒绝的一件事

我拒绝了粒子边缘光晕：它让程序卡顿，也偏离了清晰、半透明点云的目标，最终改用大小与透明度表达层次。我也要求回调过强的土星环，并将多个分散的数据图表整合为一个，减少视觉干扰。

## AI 的结果中需要纠正什么

一个反复纠正的问题是高密度模式的含义：我希望增加空间粒子，同时控制星球点的数量、紧凑程度和间距，并不希望简单放大星球或留下空间点的空隙。另一个问题是跟随行星后粒子变得过小，需要修正跟随视角下的缩放补偿，而不是只继续增加粒子大小。

还出现过加载界面不消失、点云空白、模块加载路径错误、按钮文字溢出和全屏图标被裁切等实际问题。我提供浏览器与错误信息，AI 据此修改，再由我验证效果。没有证据表明项目发生过“编造列名”或“静默删除解析失败的行”，因此不把这些示例写成已经发生的事实。

## 本次整理的取舍

原网页文件保留在仓库根目录，不移动、不改变原数据或渲染效果，避免破坏 GitHub Pages 的资源路径。`out/main.jpg` 是我提供的最终作品图，按原文件复制；它不是由新增的 Python 脚本生成。

为了补齐可复现的数据流程，AI 新增了一份 NASA/JPL 原始网页快照及其下载来源、时间和 SHA-256。`plot.py` 只从这份快照读取八颗行星的平均半径与恒星公转周期，另存为 `out/planetary-data.svg`。脚本根据真实表头与单位取列，使用中心值，保留原文件中的误差与参考文献；缺失行星、重复行、非法数值或校验失败会报错，不会静默跳过。该补充图不替换原网页的整合图表，也不宣称原网页的所有数值都来自这次下载。

## 工作量与时间记录

截至本次整理前，网页仓库共有 70 次提交，时间从 2026 年 9 月 10 日到 9 月 17 日，跨约一周；此前的桌面版探索不包含在这个范围内。工作集中在视觉方向、粒子参数、布局、运动规则、交互和兼容性的多轮反馈与修改。提交次数不等同于实际工时。

当前没有完整、可核对的累计 token 或工时记录，因此不填写估计 token 总数或虚构小时数。我的主要贡献是创意、审美判断、需求澄清与验收；AI 的主要贡献是把这些判断转化为代码，并协助调试与整理项目。
